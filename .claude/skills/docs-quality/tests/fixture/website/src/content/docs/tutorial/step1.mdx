---
title: step 1 — 動かす
description: 最小の例を1つ動かし、そこから何が足りないかを見る。
---

まず動かす。

```bash
go run ./tutorial/step1-clean
```

`concept:sight` が運ぶのは、その席が見てよいものだけである。判断の材料は
コーパス——後から読み返せる対局記録の集まり——に残る。

詳しくは[用語の整理](/architecture/terms/#記録と方策の語)を読むとよい。

![実在する静的画像](/images/fixture.txt)

## 確かめ方

`TestFixtureClean` がこの主張を固定している。

## まだないもの

記録がない。それは次のステップで扱う。
